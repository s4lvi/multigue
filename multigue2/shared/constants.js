const TILE_SIZE = 32;
const CHUNK_SIZE = { width: 128, height: 128, depth: 16 };
const PLAYER_RADIUS = 1.5; // Player's interaction radius in blocks
const LAYERS = 2; // Number of layers in the world

module.exports = { TILE_SIZE, CHUNK_SIZE, PLAYER_RADIUS, LAYERS };
